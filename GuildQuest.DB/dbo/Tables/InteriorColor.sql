CREATE TABLE [dbo].[InteriorColor] (
    [InteriorColorID] INT          NOT NULL,
    [InteriorColor]   VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_InteriorColor] PRIMARY KEY CLUSTERED ([InteriorColorID] ASC)
);

